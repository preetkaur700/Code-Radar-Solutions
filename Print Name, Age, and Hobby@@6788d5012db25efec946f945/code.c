#include <stdio.h>
int main()
{
    char Name[20];
    int Age[4];
    char Hobby[20];
    scanf("%s %d %s",name, &age, Hobby);
    printf("Name: %s\n, Age: %d\n, Hobby: %s", Name, Age, Hobby);
    return 0;

}